"regex.dc" import

var pattern
"([[:digit:]]+)[[:blank:]]+(things)"
pattern !

var phrase
"I have 1024 things right here!"
phrase !

pattern @ phrase @ 0 0 match
  "1024 things"
str= assert

pattern @ phrase @ 0 1 match
  "1024"
str= assert

pattern @ phrase @ 0 2 match
  "things"
str= assert
